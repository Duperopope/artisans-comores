# Legacy GitHub Pages workflow — disabled since the CMS requires SSR (Vercel).
# Kept for reference only.

name: Deploy Next.js to Pages (disabled)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is disabled. Remove this input to re-enable."
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub Pages deployment is disabled. Use Vercel for this project."
